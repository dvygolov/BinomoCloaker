{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch in Firefox (Admin)",
            "type": "firefox",
            "request": "launch",
            "url": "http://localhost:8080/admin/",
            "webRoot": "${workspaceFolder}",
            "preLaunchTask": "Launch PHP Server"
        }
    ]
}